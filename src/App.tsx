import {useEffect,useState} from 'react';import {Landing} from './pages/Landing';import {Exam} from './pages/Exam';import {Report} from './pages/Report';import type {Attempt,Question} from './types';
export default function App(){const [screen,setScreen]=useState<'landing'|'exam'|'report'>('landing'),[mode,setMode]=useState<'official'|'study'>('official'),[result,setResult]=useState<{a:Attempt;qs:Question[]}>();useEffect(()=>{const key=(e:KeyboardEvent)=>{if(e.key==='Escape')return;e.altKey&&e.key==='ArrowRight'&&document.querySelector<HTMLButtonElement>('[data-next]')?.click()};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key)},[]);if(screen==='landing')return <Landing start={m=>{setMode(m);setScreen('exam')}}/>;if(screen==='report'&&result)return <Report attempt={result.a} questions={result.qs} restart={()=>setScreen('landing')}/>;return <Exam mode={mode} onExit={()=>setScreen('landing')} onFinish={(a,qs)=>{setResult({a,qs});setScreen('report')}}/>}
