import type { Question } from '../types'; const labels=['A','B','C','D'];
export function QuestionCard({question,value,onChange,showResult}:{question:Question;value?:string;onChange(v:string):void;showResult?:boolean}){return <article className="card p-5"><p className="text-sm text-aws">{question.domain} · {question.service} · {question.difficulty}</p><h2 className="mt-3 text-lg font-semibold leading-relaxed">{question.question}</h2><div className="mt-5 space-y-3">{question.options.map((option,i)=>{const l=labels[i],state=showResult?(l===question.answer?' correct':value===l?' wrong':''):value===l?' selected':'';return <button key={l} onClick={()=>onChange(l)} className={'focus-ring option'+state} aria-pressed={value===l}><b className="mr-3 text-aws">{l}.</b>{option}</button>})}</div>{showResult&&<div className="mt-5 rounded bg-slate-900 p-4 text-sm"><b>Explicação: </b>{question.explanation}</div>}</article>}
